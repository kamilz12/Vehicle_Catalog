
#include "Vehicle.h"

Vehicle::Vehicle(const std::string &brand, const std::string &model, const int vehProductionYear, const int wheelCount)
        : brand(brand), model(model), vehProductionYear(vehProductionYear), wheelCount(wheelCount) {}
